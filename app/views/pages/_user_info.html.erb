<div class="user_info">
  <a href="<%= user_path(current_user) %>">
    <div class="user_name">
      <%= h current_user.name %>
    </div>
    <div class="microposts">
      <%= pluralize(current_user.microposts.count, "micropost") %>
    </div>
  </a>
</div>